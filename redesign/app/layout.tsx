import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Buy1Minute — Own One Minute of the Internet",
    template: "%s · Buy1Minute",
  },
  description: "1,440 scarce minutes. Own one permanently and take over the global homepage for 60 seconds every day.",
  openGraph: {
    title: "Buy1Minute — Own One Minute of the Internet",
    description: "Pick your minute. Broadcast daily. Own the internet clock.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
